import { LigneIngredient } from "./ligne-ingredient";
import { Recette } from "./recette";
import { Resultat } from "./resultat";


describe('Recette', () => {
  it('should create an instance', () => {
    expect(new Recette()).toBeTruthy();
  });
});
